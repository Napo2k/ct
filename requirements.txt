# ClaudeTrader cycle runner
mcp>=1.0.0
anthropic>=0.40.0
tzdata>=2024.1  # Windows: ZoneInfo requires this; no-op on Linux/macOS

# MT5 MCP server (Windows only — install separately on Windows host)
# MetaTrader5>=5.0.45
# fastmcp>=2.0.0
